# kusanagi — Agent 间协作网络设计提案 v3

> **v3 相对 v2 的差异**：项目定名 `kusanagi`（§6）；Haskell 拿到一个明确且封闭的位置（§5）；v2 结尾待裁的三件事按最佳实践定下（§7）；补上完整的 seam 表（§4.5），因为「完全模块化」必须是一张可核对的表而不是一句形容。
>
> v2 的论点未变，不重复：**死信箱是基底、直连是优化**（§1）；隐私拆成四件事逐条交代（§2）；「高性能」在 LLM 时间尺度下的真实含义是每 agent-hour 的钱与常驻内存（§3）。
>
> v1 的三处前提纠正全部保留：不自研密码学、权限只以可衰减授权存在、shipped 产物只有一个 Rust 二进制。
>
> **v3.1 的核实**：名字占用、依赖存在性、条件写入支持、成本模型、上下文预算，全部实测过一遍。三处我写错或写松的已改（MinIO 的条件写、成本参照宿主、阶段编号与 Amazon S3 撞名）；一处我自己的建议被自己的验证推翻，记在 §9。
>
> 状态：提案。第一段代码落地时，本文拆成 `ARCHITECTURE.md` 与各 crate 的 SPEC，本文即废止。

---

## 0 我发散过的六个框架

| 框架 | 世界观 | 结论 |
|---|---|---|
| **A. Overlay 网络** | endpoint、会话、成员协议、打洞 | **淘汰为基底**。会话状态是内存开销的来源；打洞在 CGNAT 与劣质移动网上最不可靠。保留为可选适配器 |
| **B. 日志复制** | 没有「消息」，只有各自的 append-only 日志互相复制 | **保留为载荷形状**。去重、定序、重放、分叉检测全部免费 |
| **C. 黑板 / tuple space** | 没有收件人，只有事实与模式匹配 | 淘汰。带宽 O(n)，且加密后无法模式匹配 |
| **D. 死信箱** | 没有连接。写到对方会去看的地方，轮询自己的地方 | **胜出，成为基底** |
| **E. 全广播 + 试解密** | 一切进公共流，收件人扫描 | 淘汰。O(n) 带宽。学术解法见 §4.4 |
| **F. capability 即地址** | 没有身份层，持有即权限 | **部分吸收**：地址由共享秘密派生；审计与撤销仍需签名的 Grant 链 |
| **G. git 即基底** | 每个 cohort 一个 git 仓库，消息即 commit | 淘汰为基底（加密 blob 永久膨胀），**保留为一个 Waypoint 适配器** |

被淘汰的理由本身是设计约束，所以记在这里。

---

## 1 论点：把 p2p 与死信箱的主从关系倒过来

> **死信箱是基底，直连是优化。**

**其一，你已经接受了基础设施。** 需求 3 明说要把工作空间外包到云服务器 / 对象存储 / 公司集中服务器。既然那台宿主一定存在，再为「无宿主」情形建一套打洞、中继、成员协议，是为一个你并不身处的世界付钱。

**其二，离线是常态而非异常。** 偏远地区、手机、免费云主机，加上 Agent 本身的节奏（一次 turn 思考几十秒、可能几小时不动），意味着「对方此刻在线」是罕见事件。在死信箱模型里，**「离线」根本不是一个需要处理的状态**——需求 8 从一个功能退化成默认行为，不需要任何代码。

**其三，消息传输与工作空间是同一个机制。** 工作空间已经是「放在对象存储里的、按内容寻址的字节」，消息也放那里，就不需要第二套东西。v1 的 `carry` + `post` + `depot` 三个 crate 在此塌缩成一个半。

代价是延迟下限等于轮询间隔，由 §3 处理。**直连不是被删掉，是被降级成一个 Waypoint**：对方可达时，「对方进程本身」就是那个 Waypoint，协议一个字节不变，延迟等于一个 RTT。一套机制，两种摆放位置——好的抽象让快路径成为慢路径的特例，而不是它的旁路。

---

## 2 「强隐私」必须先拆成四件事

| # | 属性 | v3 达到 | 靠什么 |
|---|---|---|---|
| 1 | **内容机密性** | 完全 | 标准 AEAD。谁在这里创新谁就是在制造漏洞 |
| 2 | **谁与谁通信（对宿主）** | **完全不可链接** | Drop 地址 = `HKDF(共享秘密, 序号)`，每条一个新地址，永不重复 |
| 3 | **网络成员规模** | 仅在哑对象存储上部分泄漏 | Bell（§3.2）；能长轮询的宿主不需要 Bell，见 §7 D1 |
| 4 | **流量分析（何时、多大）** | 由 `veil` 承担 | 定长分桶填充 + 时序抖动；可选 SOCKS 适配器 |

**v1 没有解决第 2 项**——overlay 里的中继直接看见 A→B。这是 v2/v3 相对 v1 最大的实质提升，而它是**免费的**：不可链接的地址派生比维护会话更简单，不是更复杂。

顺带得到一个我没预期的性质：

> **不可链接的死信箱同时是不可垃圾投递的死信箱。**

要给你写信，必须知道你的 Drop 地址；要知道地址，必须持有共享秘密；要持有共享秘密，必须先拿到一份 Grant。于是**未经引荐的通信在构造上不可能**，而不是被过滤掉。需求 5 的「防注入」在网络层就此关闭——它不依赖判断力，所以不会被说服。

**残余泄漏，说清楚**：宿主能按 IP 记录「某个 IP 在何时读写了某些随机 key」。它不知道内容、对端、关系，但知道你活着、你的活跃度、你的网络位置。缓解手段是 `veil` 或 SOCKS 适配器；我**不打算**上 PIR 或混淆网络，代价与轻量目标直接冲突。

---

## 3 「高性能」在这里到底指什么

> **当消费者是一个要思考 2–60 秒的模型时，网络延迟从 200 ms 变成 2 s 是测不出来的。**

为 RPS 和 p99 优化一个 agent 网络，是在优化一个没人观测的量。真指标是另外四个：

| 指标 | 目标 | 为什么它才是真指标 |
|---|---|---|
| **每 agent-hour 的请求数与钱** | 静息 60 次条件 GET/小时；**在 R2 免费额度内 231 个 agent 成本为零** | 免费套餐用户付不起别的 |
| **常驻内存** | 空闲 RSS ≤ 24 MiB；**one-shot 模式 = 0** | 手机与 1 GB 云主机 |
| **一台机器托管多少 endpoint** | 一个守护进程多租户，1 GB 机器 ≥ 200 个身份 | 决定小公司能不能跑起 agent 规模 |
| **设备唤醒到第一条消息** | ≤ 1 s（一次条件 GET） | 「每天在线 30 秒」 |

算法公开以便复核：每分钟一次轮询 = 43,200 次读/月/agent。两家的挂牌价：

| 宿主 | 读 | 写 | egress | 免费额度 | 每 agent 每月的读 |
|---|---|---|---|---|---|
| AWS S3 Standard | $0.0004 / 1000 | $0.005 / 1000 | $0.09/GB | 无 | **$0.0173** |
| Cloudflare R2 Standard | $0.36 / 百万（Class B） | $4.50 / 百万（Class A） | **免费** | 10M 读 + 1M 写 / 月 | **$0.0156**，或 **$0**（见下） |

**参照宿主定为 R2，不是 S3。** 三个理由，都是算出来的：R2 每月免费 1000 万次 Class B，按 60 秒轮询恰好覆盖 **231 个 agent**；轮询放宽到 5 分钟则覆盖 **1,157 个**——**一个中小公司的整张网络，钱是零**。免费的 100 万次 Class A 又覆盖每月 100 万条消息。而 egress 免费这一条，对 §4 的 `depot`（把工作空间外包到对象存储）是决定性的：S3 的 $0.09/GB 出站会让「共享工作空间」变成一张随用量增长的账单。

无论哪家，**读比写便宜一个数量级**（S3 是 12.5 倍，R2 是 12.5 倍）。所以整套设计必须把轮询压在读上，把写留给真实消息——**这个比价是 Bell 存在的全部理由。**

### 3.1 无常驻状态法则

> **守护进程只是缓存。杀掉它，不改变任何结果。**

所有 verb（`send` / `poll` / `recv` / `sync`）必须能作为一次性命令跑完退出，状态全在磁盘上。`serve` 只是省掉进程启动与连接复用的优化。

这条法则的价值不在省内存（虽然它把空闲内存降到零），而在于**它强迫所有操作幂等、所有状态持久**——那正是恶劣网络下本来就需要的性质。且可机械检验：

> **闸门：集成测试在每两步之间 `kill -9` 守护进程，结果必须逐字节相同。**

### 3.2 Bell：把轮询成本压成 O(1)

朴素做法是每次轮询探测一个地址窗口，成本随对端数线性增长。**Bell** 是每个 cohort 一个小对象，内容是最近变为非空的那些 Drop 地址的 **Bloom 过滤器**。一次轮询 = **一次带 `If-None-Match` 的条件 GET**，绝大多数返回 304 几乎免费；只有自己期待的地址命中 Bloom 时才真去取。

数字：1000 条/小时、1% 误判 → Bloom 约 **1.2 KB**，误判让你多花 1% 的 GET。发送方用 `If-Match`（ETag 乐观并发）更新 Bell，冲突重试；量大时按地址前缀分片。

Bloom 里的地址本身是不可链接的随机值，**公开它不泄漏关系**——测试候选地址需要共享秘密。它泄漏的只有活跃量级，即 §2 第 3 项。

### 3.3 一次写入，靠对象存储自己保证

Drop 地址确定，被攻陷的节点可能想覆写同一地址。**AWS S3 在 2024 年 8 月加入条件写入**：`If-None-Match: *` 表示「仅当该 key 不存在时才写入」（AWS S3 用户指南 *Conditional writes*）。于是 **Drop 是一次性写入的**，覆写在存储层被拒，我们不实现任何冲突解决。

**但这条支持不能假设，我上一版写松了。** 逐家核实的结果：

| 宿主 | `If-None-Match: *` on PutObject | 依据 |
|---|---|---|
| AWS S3 | ✅ | 用户指南 *Conditional writes* |
| Cloudflare R2 | ✅ 条件失败返回 `412 PreconditionFailed` | R2 S3 扩展文档明列 PutObject 支持四个条件头 |
| MinIO | ⚠️ **语义分歧** | minio#20346：MinIO 只接受精确 ETag，**不接受通配符 `*`**；minio#21526：对不存在的 key 用 `If-Match` 时 MinIO 忽略该条件，而 S3 返回 404 |

MinIO 那两条分歧的方向都很危险——**条件被静默忽略，写入照常成功**。一个把「覆写会被拒」当成前提的协议，碰上这种宿主会安静地失去一次性写入语义，而不是报错。

结论不是把 MinIO 排除，是把这件事变成机制：

> **`kusanagi doctor <waypoint>` 在接入时探测宿主，而不是相信文档。**

它实测四件事：条件写是否真的拒绝覆写、条件读是否返回 304、ETag 是否稳定、TTL 规则是否生效。然后它**签发一份宿主能力证书**，声明这台宿主可用于哪一档：一次性写入（完整语义）、或降级为「双方互相 ack 首次所见」。降级是被命名、被记录、可查询的一种状态，不是一次沉默的退让。

这也顺带成了 AX 的样板：`doctor` 的失败输出带着失败的动作、宿主、稳定错误码，以及能恢复它的那条确切命令。

### 3.4 序号即链高

`addr(n) = HKDF(ss, "drop"‖n)`，而段 n 的 `prev` 指向段 n−1 的哈希——**寻址结构与完整性结构是同一个索引**。这类塌缩顺带给出去重、定序、重放、分叉检测四件事，零额外代码。

---

## 4 架构

### 4.1 六个词

| 词 | 它是什么 | 冻结了什么 |
|---|---|---|
| **Segment** | 唯一会旅行的东西：一段封好的、哈希链上的字节 | 没有「消息」这个中间概念。段即事件即消息 |
| **Drop** | 一个不透明地址，恰好落一个 Segment，一次性写入 | 地址不重复，因此关系不可链接 |
| **Waypoint** | 任何能按 key 存取字节的东西：S3、公司 HTTP 盒子、git remote、目录、U 盘、**对方进程本身** | Waypoint 按构造不被信任，校验只靠哈希 |
| **Bell** | 每 cohort 一个小对象，最近非空 Drop 的 Bloom 摘要 | 轮询成本 O(1) 且与对端数无关 |
| **Grant** | 可离线验证、只能衰减、自带过期的授权 | 权限只以此形式存在。撤一条边 = 整棵子树失效 |
| **Cohort** | 一份签名的成员名册 + 一个根钥；名册版本即 epoch | 隔离 = 发布不含那一位的新名册。**没有 gossip 协议** |

**两个词改了名，理由是名字换了。** v2 用 `Writ`（通行文书）和 `Caravan`（商队），那是 `postroad`／驿路那套隐喻撑起来的；隐喻一撤，`Writ` 只剩下一个需要查词典的罕见法律词，`Caravan` 只剩下一个与机制无关的联想。`Grant` 和 `Cohort` 是每个工程师都认识的普通词，且各自只指一件事。**一个决定被推翻时，靠它站着的东西必须一起复查**——这就是那次复查的结果。

### 4.2 crate 图与行数预算

```
kernel     ~1000   纯类型：Handle / Grant / Drop / Segment / Epoch。零 I/O
chain       ~700   哈希链段：追加、验证、分叉检测
seal        ~900   封装 + 地址派生。全是标准件的装配
veil       ~1000   混淆 seam ＋ 两个适配器（自研 baseline / 外部 PT 或 SOCKS 子进程）
drop       ~1200   死信箱协议：一次性写入、Bell、窗口、TTL
waypoint   ~1600   适配器：目录 / S3 / HTTP 盒子 / git remote / 直连对端
grant      ~1100   签发、衰减、验证、撤销、名册
depot       ~900   内容寻址的工作空间，走同一批 Waypoint
port       ~1100   本机 socket 门面 ＋ MCP 门面
kusanagi   ~1200   CLI ＋ 唯一装配点（唯一取时钟、唯一发种子、唯一起任务）
xtask       ~700   机械闸门
─────────────────
           ~11400
```

比 v1 的 16,200 行少约三成，**少的是最难的三成**——gossip 成员协议与 NAT 穿透整个消失。更好的基底应当**删掉**子系统，而不是让它们变整齐。

预算仍为全仓 Rust ≤ 25,000 行、单 crate ≤ 2,500 行，`xtask budget` 判红。

**这个数字现在是实测出来的，不是估的。** sprawling 的 Rust 源码是 91,353 行 / 3,426,368 字节 → **37.5 字符/行**（实测）。按代码 3.0–3.5 字符/token 折算：

| 规模 | 字节 | tokens | 占 1M 上下文 |
|---|---|---|---|
| sprawling 全量 | 3.43 MB | 0.98–1.14 M | **98%–114%——已经装不下** |
| 本项目预估 11,400 行 | 0.43 MB | 122k–143k | 12%–14% |
| 预算上限 25,000 行 | 0.94 MB | 268k–313k | 27%–31% |

实测的是字符/行，估的是字符/token——分开说，因为前者可复核而后者不能。**sprawling 装不进 1M 这件事现在有数字了**，它是本项目必须另起而不是扩建的量化理由。25,000 行留下约七成上下文给推理、文档和 diff，那才是「读得完」的真实含义。

### 4.3 插件接口塌缩成三个纯函数

v1 的插件挂在会话上，而会话是状态机，状态机是插件 ABI 的坟场。v3 里企业要替换的东西全是**无状态纯函数**：

```rust
fn seal(key: &Key, plain: &[u8]) -> Vec<u8>;
fn open(key: &Key, sealed: &[u8]) -> Result<Vec<u8>, OpenFailed>;
fn derive(secret: &Secret, index: u64) -> (DropAddr, Key);
```

加上可选的 Waypoint 适配器（`put_if_absent` / `get`）与 `veil` 适配器。**插件仍是独立进程**，讲 stdio 上的这几个动词——读不到你的密钥、改不了你的状态、崩溃只掉一条链路，用什么语言写都行。

### 4.4 特意没有采用的东西

- **FMD（eprint 2021/089）/ OMR（2021/1256）**：让不受信服务器代为筛选收件。它们解决的是「收发双方**没有**共享秘密」的场景（公开支付系统）。我们有——引荐是前置条件——所以确定性派生地址在我们的场景里**既更强也更便宜**：零误判、零服务器计算。arXiv 2109.06576 已指出 FMD 的误判率给出的隐私保证本身是模糊的。
- **MLS（RFC 9420）**：逐对 Drop 链已够。等某个 cohort 真需要 O(log n) 群重钥时，作为 `seal` 的可选适配器进来。
- **DHT / 区块链发现**：一张公开的可链接关系图，与 §2 第 2 项直接对立。
- **混淆网络 / PIR**：与轻量目标直接冲突。用 SOCKS 适配器把它外包给已有的匿名网络，约 100 行。

### 4.5 「完全模块化」是这张表

你的既有规矩：**一个适配器是假想的 seam，两个才是真的。** 所以每个 seam 都带第二实现，外加一套**替换实现必须通过的 conformance suite**——需求 1 的完成判据是这个，不是一份接口文档。文档会漂移，测试不会。

| Seam | 声明于 | 生产适配器 | 第二适配器 | conformance 断言什么 |
|---|---|---|---|---|
| `Waypoint` | kernel | R2 / S3 / HTTP 盒子 | 本地目录 | `put_if_absent` 的并发语义；覆写必须被拒。**由 `doctor` 对真实宿主实测，不读文档**（§3.3） |
| `Seal` | kernel | ChaCha20-Poly1305 | 恒等桩（明文直通） | 上游测试向量 + 恶意字节 fuzz |
| `Derive` | kernel | HKDF-BLAKE3 | 计数器桩 | 地址无碰撞；任意两地址不可关联 |
| `Veil` | kernel | 自研 baseline | 外部 PT / SOCKS 子进程 | 输出与随机流不可区分；主动探测无回应差异 |
| `Store` | kernel | redb | 内存 | `kill -9` 后逐字节相同 |
| `Clock` | kernel | OS 时钟 | 确定性测试钟 | 唯一采样点在 `kusanagi::assembly` |

**故意不做 seam 的**：`chain` 与 `grant` 各只有一个实现，且它们是规则本身——给规则做接口等于邀请第二权威。`drop` 的协议同理。

### 4.6 最佳实践：继承，而不是重新发明

Rust 侧的硬规矩整套继承 sprawling 的 `AGENTS.md`，不重写：非测试代码禁 `unwrap`/`expect`/`panic!`/裸索引切片，检查型算术，禁 `as` 窄化，禁 `unsafe`，一模块一文件，默认 `pub(crate)`，时间作为参数传入。这些已经由 lint 与 `xtask` 闸门执行，搬过来即可。

lint 抓不到、要靠评审的四条：**为绕过借用检查而 clone 是所有权设计缺陷，不是风格问题**；线路热路径上用 `&str`/`Cow<str>` 而非 `String`；带不变量的字段一律私有（六个域类型全是私有字段的 newtype）；长函数拆方法。

有一条通行建议我**不采纳**：「巨型 enum 说明缺抽象，应改成 trait + 类型」。你的架构原则是「穷尽 enum 优于布尔标志」且「trait 只在已有第二实现的 seam 上引入」。两者冲突时以你的原则为准——巨型 enum 的正确疗法是把分支体抽成方法，而不是把一个封闭集合换成一个开放集合。**把封闭的东西开放掉，正是 §0.4 里我拒绝让 Agent 自行发明防御动作的同一个错误。**

---

## 5 Haskell 的位置

你说 Haskell 是探索性的、可以用在特别适合的模块。我接受，但要把边界定死——否则它会漂成第二权威，而那是你自己 `AGENTS.md` 明令禁止的。

### 5.1 准入条件

一个位置要能用 Haskell，必须同时满足三条：

1. **它不随产品交付。** 出货物永远是一个 Rust 二进制。装 `kusanagi` 的人、以及改 Rust 的人，都不需要 GHC。
2. **它不定义任何 Rust 也定义的规则。** 重述一遍规则就是第二权威，无论重述得多优雅。
3. **它消失了，网络照跑。**

### 5.2 唯一合格的位置：`adversary/`

一个**黑盒对抗性性质预言机**：仓外的 cabal 工程，不在 Rust workspace 里，不在发布物里，不在依赖清单里。

**为什么这里 Haskell 真的更好，而不是装饰：**

其一，要断言的东西是**轨迹之间的关系**，不是期望输出。「对任意前缀，一次撤销之后接任意后缀，都不得允许被撤销的动作」在动态逻辑里是一句话，在命令式代码里是一段。

其二，`quickcheck-dynamic`（IOG 与 QuviQ 合作，从 Plutus 测试里抽出的通用部分）把 `StateModel`（可能发生什么）与 `RunModel`（怎么对真实系统做）分成两个类型——**那个分界就是黑盒边界，由类型强制，不靠自律**。

其三，也是决定性的一条：动态逻辑能写**有方向的**对抗场景——「任意前缀，然后这个具体攻击，然后任意后缀」。**只会均匀随机生成的东西是 fuzzer，不是对手**；安全缺陷在轨迹空间里从不均匀分布。

**工具取舍要说实话**：`quickcheck-dynamic` 继承 QuickCheck 的手写 shrinker，而下面第 4 条规矩让「最小化的反例轨迹」成为交付物，收缩质量因此是硬指标——这恰是 Hedgehog + `hedgehog-lockstep` 的集成式收缩更强的一轴。我仍选 `quickcheck-dynamic`，因为定向对抗的表达力比收缩便利更贵；但设一个复查点：**第一个真实反例出来时，如果收缩结果不可读，就换 Hedgehog。** 这是一个有期限的赌注，不是一个信仰。

### 5.3 四条防漂移规矩

1. **它走产品自己的门。** 只讲 `port` 的 JSON schema——harness 讲的同一套。不 FFI、不链接、不共享类型。推论：它**永远无法测试用户到不了的路径**。
2. **它只陈述性质，不陈述行为。** 禁止出现「期望输出是 X」；允许「输出 A 与输出 B 满足如下关系」。重实现一条规则，就是第二权威的定义。
3. **它不能让 Rust 的闸门变红。** 独立的定时任务。否则每个 Rust 贡献者都得装 GHC，需求 2 的「一个 Agent 24 小时内完成一个模块」当场作废。
4. **它的产出是一个 Rust 测试。** 找到反例，交付物是**最小化后序列化成 Rust 回归测试**、提交在 Rust 代码旁边的那个文件。

第 4 条是承重的：**Haskell 发现问题，Rust 记住问题。** 知识永远向 Rust 迁移，Haskell 就永远积累不出权威。

### 5.4 优先打击的四个目标

| 目标 | 要证伪的性质 | 为什么值得 |
|---|---|---|
| `grant` | 衰减永不扩权；撤销立即且传递 | 安全上收益最高 |
| `drop` | 任意两个派生地址不可关联、不碰撞 | **这就是 S1 的闸门**，隐私主张的真假在此 |
| `chain` | 重放与乱序不改变最终链头；分叉必被检出 | 一致性 |
| `waypoint` | 面对**恶意宿主**——乱序、丢弃、重复、重放、谎报 ETag——不变量仍成立 | 对手策略空间上的不确定性选择，正是 Haskell 生成器的主场 |

最后一行是 Haskell 最强的理由：**为一个撒谎的宿主建模，本质是在一个策略空间上做不确定性选择。**

### 5.5 你说的「探索性」，我给它一个有终点的用法

`grant` 的代数——衰减是格上的交，要保证 `attenuate(g,c) ⊑ g`、幂等、复合下不扩权——在 Haskell 里迭代便宜，在 Rust 里迭代贵。**在 Haskell 里把代数试对，转写成 Rust，Haskell 只留下那几条定律。** 终点明确：代数稳定之日，Haskell 侧的求值器删除，只留 law checker。

GHC 目前不在这台机器上（`ghc: command not found`）。这不阻塞任何事——正是上面第 3 条规矩的目的。

---

## 6 命名：kusanagi

定名 `kusanagi`，二进制同名，不设简称（一个概念一个名字）。

**它与我上一轮那条规矩不冲突，因为那条规矩管的不是这里。** 我说过「罕见词是成本」，那约束的是**词汇**——Segment、Drop、Waypoint、Bell、Grant、Cohort，它们出现在每一句文档和每一条命令里，必须字面、普通、精确。**项目名是一个标签，学一次就够**：`kubernetes`、`kafka`、`nginx` 没有一个是可理解的。所以规矩是：**标签可以有意象，词汇必须是字面的。** 这也意味着我不会把六个域名词改成典故——混用隐喻的代价，正是 §4.1 刚刚付过的那一笔。

意象本身是站得住的，而且不止一层：草薙剑是为了从合围的野火里烧出一条隔离带而挥出去的——**烧隔离带正是 §0.4 的隔离动作**；而《攻壳机动队》的「Stand Alone Complex」讲的是**没有原本、没有中心的个体之间涌现出的协同**，那就是这份设计本身。README 里值得写一句，不值得写一段。

**尽职调查，已实测**（上一版我把 crates.io 的 403 当成了不确定，那是它拦裸 UA；带正常 User-Agent 重查的结果如下）：

| 位置 | 状态 | 说明 |
|---|---|---|
| **crates.io `kusanagi`** | ✅ **可用**（404） | `kusanagi-core` / `-kernel` / `-net` 同样可用。**不需要退让到前缀** |
| **`github.com/2youg1/kusanagi`** | ✅ 可用（404） | 仓库路径干净 |
| `github.com/kusanagi` 组织 | ❌ 已被占用 | Prime Strategy 的 KATANA/KUSANAGI 多语言 SDK。**放弃同名组织，用你自己的账号路径** |
| npm `kusanagi` | ❌ 已被占用 | 本项目纯 CLI 不发 npm 包，不构成阻碍 |

同名产品的规模要说清楚：`KUSANAGI` 是 Prime Strategy（东京）2015 年发布的 WordPress 高速执行环境，宣称覆盖 26 家云服务、累计十万次部署，官网 `kusanagi.tokyo`；另有 `JZacharie/Kusanagi`，一个 Rust 写的 Kubernetes 监控平台。

**我的判断：可以用。** 唯一被占的关键位置是 GitHub 组织名，而你本来就会用 `2youg1/kusanagi`。真实代价只剩搜索可见度——这个代价由 README 第一句承担，不由架构承担。

---

## 7 v2 遗留的三件事，按最佳实践定下

### D1 Bell 换不换 — **定为：Bell 是 Waypoint 的一项能力，不是协议的必需**

哑对象存储（S3/R2）上启用 Bell：轮询 O(1)，代价是宿主看得见该 cohort 的活跃量级。能长轮询的宿主——那个约 200 行的公司服务器——**不需要 Bell，也就不泄漏任何东西**。

于是隐私代价只由「选择了哑存储」的人承担，且按 cohort 可关。**最佳实践是把取舍推到用户已经做出选择的那根轴上**，而不是替所有人二选一。

### D2 一百万 Agent 是扁平还是分层 — **定为：分层**

1000 个 cohort × 1000 人，跨 cohort 靠可传递的 Grant。真要扁平全局可达，gossip 成员协议与 NAT 穿透就得加回来（11.4k → 16k+ 行），而且 §2 第 2 项的不可链接性会破——**扁平可达要求一张全局可解析的名字表，那张表就是关系图**。公司边界本来就是天然的 cohort 边界；扁平语义没有对应的现实需求。

### D3 是否强制一个 baseline suite — **定为：强制，插件只做增量**

照 TLS 的做法（强制实现的基础套件）。在 v3 里这一条比 v1 更尖锐，因为 `derive` 也是可插拔的：**两个用不同 `derive` 的 endpoint，连对方的信箱在哪都算不出来**——那不是降级，是彻底分区。baseline 是网络之所以是一张网络的条件。

---

## 8 落地路线

阶段编号用「阶段 n」，提交仍写 `card-<stage>.<index>`。**上一版我把阶段叫 S0–S7，于是「S3 云端」同时指第三阶段和 Amazon S3**——在一份满篇讨论对象存储的文档里，那是一个名字指两件事，正是你用 `xtask lexicon` 禁止的东西。我自己犯了它。

| 阶段 | 内容 | 收口条件（可执行） |
|---|---|---|
| **0 骨架** | `kernel` + `chain` + 目录 Waypoint | 同机两个身份经一个**本地目录**投递一段，链可离线验证。**无网络、无密码学** |
| **1 死信箱** | `drop` + `seal` 的地址派生 | 一百段经派生地址往返；**宿主日志里任意两条记录不可关联**——这是断言，不是说法 |
| **2 权限** | `grant` | 三级衰减链，撤第二级则第三级立刻失效；`kani` 证明「衰减不能扩权」 |
| **2′ 对手** | `adversary/` 起步，先打 `grant` 与 `drop` | 第一个反例被最小化并落成 Rust 回归测试。**同时验收 §5.3 的四条规矩** |
| **3 上云** | 对象存储 Waypoint + Bell + `doctor` | **在真实 R2 上跑通**（免费额度内），S3 与 MinIO 各跑一遍 `doctor`；实测每 agent-hour 请求数写进 SPEC |
| **4 密封** | `seal` 全量 + 混合 PQ | 测试向量对齐上游；恶意字节 fuzz |
| **5 遮蔽** | `veil` + PT/SOCKS 桥 | 主动探测无法区分握手与随机流；两个适配器过同一套 conformance suite |
| **6 接入** | `port` + `kusanagi join` | 未读过本仓代码的人照一页纸接入并收发；过程中随机 `kill -9`，结果不变 |
| **7 空间** | `depot` + 公司自建 HTTP 盒子 | 同一工作空间落在 R2 / 公司服务器 / 本地盘，字节逐一相同 |

阶段 0–1 是全部风险。**阶段 1 的收口条件是整个项目的试金石**：若宿主日志能把两条记录关联起来，隐私主张就是假的，应当停下重划而不是继续往上垒。

## 9 一处被验证推翻的建议

上一轮我提议把那台约 200 行的长轮询公司服务器提到阶段 1 之后，理由是它在成本和隐私两轴都严格优于哑对象存储。**验证之后我撤回这个提议。**

推翻它的是 R2 的免费额度：60 秒轮询下 231 个 agent、5 分钟轮询下 1,157 个 agent，**成本为零**。于是「成本」那一轴在中小规模上根本不存在差距，只剩隐私那一轴（Bell 泄漏活跃量级）。而在需求 4 面前，**「一个都不用运维」压倒「只有 200 行的服务器」**——一个零技术背景的员工能注册一个 R2 桶，他没法被要求去运维一台常驻服务器。

所以顺序不变：对象存储在前（阶段 3），公司自建盒子在后（阶段 7），作为在意活跃量泄漏、或规模超出免费额度的组织的升级路径。**我的原始判断只看了两条轴的方向，没看它们在真实规模上的量级——量级一算，结论就翻了。**

---

*本文档采用 MPL-2.0。*
